import React from "react";

//thus module open a page for a specific person
// shows person name in title
//total number of user contacts to target person
//total number of contacts from target person to user
// 2 lists of dates of contacts for user and person
// go back btn
export default function sessionStatistics({ name }) {
  return <div></div>;
}
